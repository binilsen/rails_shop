<h2 class="text-center my-2 fs-1">Sign up</h2>
<%=content_for :title,"User Registration"%>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="">
  <div class="row justify-content-center">
    <div class="col-sm-6">
        <div class="form-floating   mt-4 mb-2">
          <%= f.email_field :email, autofocus: true, autocomplete: "email" ,class:'form-control',placeholder:'Email' %>
            <%= f.label :email , class:"my-1" %>
        </div>
    </div>
  </div>
<div class="row justify-content-center">
  <div class="col-sm-6">
      <div class="form-floating my-2">
        <%= f.password_field :password, autocomplete: "new-password" ,class:'form-control' ,placeholder:'password' %>
          <%= f.label "Password " +@minimum_password_length.to_s+' characters minimum' ,class:"my-1" %>
      </div>
  </div>
</div>

<div class="row justify-content-center">
  <div class="col-sm-6">
      <div class="form-floating my-2">
        <%= f.password_field :password_confirmation, autocomplete: "new-password" ,class:'form-control',placeholder:
          :password_confirmation %>
          <%= f.label :password_confirmation, class:"my-1" %>
      </div>

  </div>
</div>
  <div class="actions text-center my-4">
    <%= f.submit  value:"Sign up",class:"btn btn-lg btn-outline-danger" %>
  </div>
  </div>
<% end %>

<%# <%= render "devise/shared/links" %>
